# Binaries for programs and plugins
*.exe
*.exe~
*.dll
*.so
*.dylib

# Test binary, built with `prometheus_go test -c`
*.test

# Output of the prometheus_go coverage tool, specifically when used with LiteIDE
*.out

# Dependency directories (remove the comment below to include it)
vendor/
/prome_exporters
/prome_exporters.yaml
/.idea